import React from 'react';
import {
    ScrollView,
    View,
    Text,
    Image,
    Button,
    StyleSheet
} from 'react-native';
import { useSelector, useDispatch } from 'react-redux';

import Colors from '../../constants/Colors';

const FilterHostScreen = props => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default FilterHostScreen;